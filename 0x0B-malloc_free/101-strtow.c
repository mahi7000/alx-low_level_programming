#include "main.h"
#include <stdlib.h>

/**
 * strtow - function
 * @str: char
 * Return: char
 */

char **strtow(char *str)
{
	int i;

	i = 0;
	while (str[i] != '\0')
		i++;

	if (str == NULL)
		return (NULL);

	return (NULL);
}
