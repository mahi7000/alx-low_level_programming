#include "lists.h"

/**
 * print_listint_safe - print
 * @head: pointer
 *
 * Return: size_t
 */

size_t print_listint_safe(const listint_t *head)
{
	size_t count;

	count = head->n;

	return (count);
}
